# /labs — 列出全部课程

读取 `labs/` 下所有 `layer-*.md` 文件，简洁列出全部层（层 0 摸底 + 层 1–8），每层给出：

- 层号 + 标题
- 一句话目标
- 最终产出物

然后告诉用户用 `/lab <N>` 开始任意一层（如 `/lab 1`），你会一步步带着做。命令语法用 Cursor 版（见 `docs/cursor-adaptation.md`）。用用户的语言回答（中文或英文，跟随用户）。
