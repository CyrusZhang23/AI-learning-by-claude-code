# 层1 — CLI 核心与会话（零基础友好）

> 可移植性：**高**（Codex 等价命令见 `docs/codex-adaptation.md`）
> 路线提示：**A 零基础**——从"先导"开始，全部步骤都做，慢慢来；**B 有基础**——快速看完先导，从步骤1 正常做；**C 进阶**——跳到文末"自测清单"，10 分钟过完。

> 🎯 **本层一句话**：Claude Code 是终端里的工具，不是网页聊天框 —— 交互式聊 vs headless 进脚本是同一个 AI 的两种入口；记忆分两层：`--continue` 续这一次对话，`#`/`CLAUDE.md` 跨所有对话。

## 目标

学会把 Claude Code 当成**终端里的工具**来用（而不只是网页聊天）。学完你能：在终端发命令、把文件喂给 AI、续接之前的对话、让 AI 记住你的偏好。

## 你将亲手产出

- `workspace/lab-01/cheatsheet.md` —— 你边做边记的"命令小抄"
- 一个能自动总结代码改动的小脚本 `summarize.sh`

---

## 零基础先导（路线A 必读，已会的可跳过）

在敲任何命令前，先搞懂三个最基本的概念——这是你从"网页小白"迈进"会用终端"的全部门槛：

1. **终端（Terminal）是什么**：就是一个让你**用打字下命令**来指挥电脑的窗口（黑底白字那种）。网页是点鼠标，终端是打字。
2. **命令是什么**：一行字，回车后电脑去执行。比如 `claude --version` 意思是"告诉我 Claude 的版本号"。`--version` 这种叫**参数**，是给命令的附加说明。
3. **Claude Code 在终端里有两种用法**：
   - **聊天式**：输入 `claude` 回车，进入对话，像网页一样聊（你现在上这门课就是这种）。
   - **一次性命令式**：`claude -p "问题"`，问一句、答一句就结束，适合写进脚本。

> 路线A 的同学：如果连"怎么打开终端""怎么粘贴命令""command not found 是什么意思"都不确定，**直接问教练**，我会一步步陪你，不要怕问。

---

## 前置检查

- [ ] 能打开终端，并且 `claude --version` 能显示版本号（不行就先回层0 装好）
- [ ] 知道怎么在终端里"粘贴一行命令然后按回车"
- [ ] 建好工作区：让教练帮你在仓库里建 `workspace/lab-01/` 文件夹

## 分步教案

### 步骤 1 — 体验两种用法（路线 A/B/C 都做）
- **做什么**：(1) 输入 `claude` 进对话，问"你好，你是谁"，看回答，然后输入 `/exit` 退出。(2) 再输入 `claude -p "用一句话说说今天适合学什么"`，看它答完就自动结束。
- **为什么**：亲手感受"聊天式"和"一次性命令式"的区别——这是 Claude Code 最基本的两种形态。
- **做对了会看到**：第一次进入对话界面能来回聊；第二次答一句就回到普通终端。

### 步骤 2 — 把命令的回答存下来（A/B/C 都做）
- **做什么**：运行 `claude -p "列出 3 个适合新手的编程语言" --output-format json`，观察输出变成了一段带括号的"结构化数据"（JSON）。
- **为什么**：加 `--output-format json` 能让回答变成"程序能读"的格式——这是把 AI 接进自动化流程的第一步。现在不用懂 JSON 细节，知道"有这么个开关"即可。
- **做对了会看到**：输出从一段自然语言变成了 `{ ... }` 形式的结构化文本。

### 步骤 3 — 把文件/改动喂给 AI（A/B/C 都做）
- **做什么**：(1) 在 `workspace/lab-01/` 里随便建个文本文件写几行字；(2) 运行 `cat workspace/lab-01/你的文件 | claude -p "用中文总结这个文件讲了啥"`。
- **为什么**：`|`（叫"管道"）的意思是"把左边的内容交给右边处理"。这就是怎么让 AI 读你的文件——把内容"喂"给它。
- **做对了会看到**：AI 的回答确实是针对你文件内容的，而不是泛泛而谈。

### 步骤 4 — 让 AI 记起刚才聊过的（A/B/C 都做）[基础·B/C 可压缩]
- **做什么**：(1) `claude -p "请记住：我最喜欢的数字是 42"`；(2) 紧接着 `claude --continue -p "我最喜欢的数字是几？"`，看它是否答对。
- **▶ 让学员先预测**：跑第二条之前先问——"如果把 `--continue` 去掉，直接 `claude -p '我最喜欢的数字是几？'`，它还答得出 42 吗？为什么？"（答案：答不出，因为没有 `--continue` 就是全新会话，没有上一次的上下文。）让学员预测完两种都跑一遍验证。
- **为什么**：`--continue` 让这次命令接着上一次的对话——AI 是有"记忆"的，对话可以续上。这是做长任务的基础。
- **做对了会看到**：带 `--continue` 答出 42；不带就答不出 —— 对照证明"续接"靠的就是这个开关。

### 步骤 5 — 让 AI 记住你的长期偏好（A/B/C 都做）
- **做什么**：进入 `claude` 对话，输入一行以 `#` 开头的话，比如 `# 回答我时尽量简洁，用中文`。然后用 `/memory` 看看记下了什么。
- **为什么**：`#` 开头是"速记"——把一条长期偏好存进记忆，以后每次对话都生效。这和步骤4 的"对话续接"不同：续接是接着这一次，记忆是跨所有对话。
- **做对了会看到**：`/memory` 里能看到你刚记下的偏好。

### 步骤 6 — 产出你的小抄和第一个脚本（A/B 必做，C 可选）
- **做什么**：
  1. **教练直接写好** `workspace/lab-01/cheatsheet.md`（把前面用过的命令整理成小抄）—— 不用学员手填。写完让学员读一遍，教练抽问 2 个："`--continue` 和 `#` 这两行，哪个是续这一次、哪个是跨所有对话？" 学员答得出 = 这步过关（验证理解靠回答，不靠填空）。
  2. 建一个 `summarize.sh`，让它先看工作区 diff、空就 fallback 到最近一次 commit：
     ```bash
     #!/usr/bin/env bash
     set -euo pipefail
     DIFF=$(git diff)
     [ -z "$DIFF" ] && DIFF=$(git log -p -1 --no-color)
     [ -z "$DIFF" ] && { echo "仓库还没有任何 commit，先 git commit 一次再来。"; exit 0; }
     echo "$DIFF" | claude -p "用中文总结这些代码改动，分两段：1) 改了什么；2) 潜在风险或需要复核的点。"
     ```
  3. `chmod +x workspace/lab-01/summarize.sh && bash workspace/lab-01/summarize.sh` 跑一下，看 AI 是否给出中文总结。
- **为什么**：把学到的固化成自己能反复用的东西。`summarize.sh` 就是你的第一个"自动化小工具"——管道（步骤3）+ headless（步骤1）的合体。
- **做对了会看到**：终端打印一段针对真实改动的中文总结，分"改了什么 / 风险点"两段。
- **🪤 容易踩的坑（▶ 先让学员预测）**：跑脚本前先问学员——"我们刚在 `workspace/lab-01/` 下建了文件，你觉得脚本里第一行 `git diff` 会抓到这些改动吗？" 多数人会答"会"。揭晓：**抓不到**。`workspace/` 在本课程 `.gitignore` 里——只改 `workspace/` 下的文件，`git diff` 永远是空的。脚本里 `git log -p -1` 的 fallback 就是为了避开这个坑：哪怕工作区干净，也能用上一次 commit 当样本验证脚本骨架。**机制点**：`git diff` 只看已追踪、未暂存的改动；新文件 / 被 ignore 的文件它都不显示。

### 步骤 7 — 全景扫描（收尾，A/B/C 都做）[核心]
- **做什么**：教练带学员跑一遍 `claude --help`，对照本层学过的开关（`-p` / `--output-format` / `--continue` / `--resume` / `--add-dir`），逐项问"这个我们碰过没？是干啥的？"，把**没碰过但存在**的开关列一个"待探索"清单（如 `--model`、`--permission-mode`、`--mcp-config`）。
- **为什么**：本层只覆盖了 CLI 入口的一小部分。主动对照完整 `--help` 查漏，建立"我知道还有什么没学"的全景感 —— 这是后面每层都会重复的习惯。
- **做对了会看到**：学员能指着 `--help` 说出哪些是本层学过的、哪些留到后面层。

## 完成标志

- 你能分清"聊天式 `claude`"和"一次性 `claude -p`"
- 你会用 `|` 把文件喂给 AI
- 你能用 `--continue` 续接对话、用 `#` 存长期记忆，并说清两者区别
- 你有了一份 cheatsheet 和一个能跑的 `summarize.sh`
- 🎯 你能复述本层那句话：交互 vs headless 是同一个 AI 的两种入口；记忆分"续这一次"和"跨所有对话"两层

学完更新 `workspace/progress.md` 的进度，然后 `/lab 2` 继续——交互模式里的 `/xxx` 斜杠命令是另一大块，**整套体系（内置 + skill/插件 + 自定义）在层 2 系统讲**，不在这一层。

---

## 可选加餐：从手机/平板远程接入（随时随地上课）

> 路线 A 可先跳过，等熟练了再回来；路线 B/C 推荐现在就配好——之后整门课都能在手机/平板上做。

Claude Code 跑在**你的电脑**上，但你不必一直坐在电脑前。配好下面这套，就能从手机/平板 SSH 进电脑、接着同一个会话继续上课（本课程作者就是这么在 iPad 上写的）：

- **Tailscale**（手机端 + 电脑端都装）：零配置组一张私有网络，手机直接 SSH 到你家/公司的电脑，**不用公网 IP、不用配端口转发**。这是远程接入的基石。
- **手机/平板 SSH 客户端**：
  - **iOS**：WebSSH；或 **Blink Shell**（内置 **mosh** 协议，切 WiFi/断网能不掉会话）
  - **Android**：Termius
  - **鸿蒙 HarmonyOS**：生态里缺好用的 SSH/Tailscale 客户端——AI 时代，建议换台手机更省心 🙂
- **tmux**（电脑上）：起一个**长驻会话** `tmux new -s study`，手机 SSH 进去 `tmux attach -t study`。断线重连进度不丢，电脑那头 Claude 会话一直活着。

> **机制点**：mosh / tmux 都只是**终端传输与会话保持**层（"更耐断网的管子"），跟 Claude 本身无关。它们解决的是"我人在手机、会话在电脑"的连接问题。这套配好，层 6 的"多设备协同"你才有真实的体感基础。

---

## 路线C 自测清单（进阶者 10 分钟过关）

能独立完成以下全部，即可跳过本层进入层2：
- [ ] 用 `--output-format json` + `jq` 从输出里提取正文字段
- [ ] 用 `--continue` 和 `--resume` 各续接一次会话，并说清区别
- [ ] 用 `--add-dir` 把额外目录纳入工作范围
- [ ] 区分 `#` 速记 / 项目级 `CLAUDE.md` / auto-memory 三种记忆的适用场景
- [ ] 写一个 headless 脚本接进 git 的 `prepare-commit-msg` 钩子
