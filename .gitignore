# 课程作者标记——只存在于作者机器，Layer 0 据此跳过学员本地化。永不进仓库
.course-author

# 个人学习记录 / 实验产生的工作区——不进公开仓库
# 注意：学员首次运行时 Layer 0 会把下面这行 workspace/ 删掉（解除忽略），
# 让 git 教学（worktree 隔离等）能作用在 workspace 上；作者机器保留忽略。
workspace/
scratch/
*.local
*.local.*
.claude/settings.local.json

# Claude Code 运行时 / 隐私数据——绝不进仓库
# projects/ 含会话历史与 auto-memory；worktrees/ 是隔离工作树；lock 是调度锁；
# agents/ 是层5 让学员亲手建的产出（不预置答案，学员自建才是教学意义）
.claude/projects/
.claude/worktrees/
.claude/scheduled_tasks.lock
.claude/agents/

# 各层实验过程中生成的临时产物（教案本身保留在 labs/，这里排除运行产物）
labs/**/output/
labs/**/.runs/

# 系统/编辑器
.DS_Store
Thumbs.db
.idea/
.vscode/
__pycache__/
*.pyc
.venv/
venv/
node_modules/
