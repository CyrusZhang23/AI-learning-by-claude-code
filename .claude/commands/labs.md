---
description: List all course layers
---

Read every `layer-*.md` file under `labs/` and show a concise list of all layers (Layer 0 placement + Layers 1–7), each with:

- Layer number + title
- One-line goal
- Final artifact

Then tell the user to start any layer with `/lab <N>` (e.g. `/lab 1`), where you'll teach step-by-step. Respond in the user's language (Chinese or English — mirror whatever they use).
